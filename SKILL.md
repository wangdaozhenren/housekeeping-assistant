---
name: housekeeping-assistant
slug: housekeeping-assistant
displayName: 家政助手
description: 家政服务比价助手——保洁/维修/上门服务，同项目同面积比价。当用户想比家政价格、保洁多少钱、上门维修比价、家电清洗、家政服务哪个平台划算、housekeeping price comparison 时使用。覆盖：京东服务、美团本地生活、58同城。服务项目分类比价（日常保洁/深度保洁/家电清洗/维修），按次/按小时/按面积计价换算，评分与口碑对比。需城市与具体服务项目信息。只做比价，不代下单、不代付款。
summary: "家政服务比价助手——保洁/维修/上门服务，同项目同面积比价。当用户想比家政价格、保洁多少钱、上门维修比价、家电清洗、家政…"
version: 1.0.5
license: MIT
tags: 家政比价,保洁,家电清洗,上门维修,京东服务,美团,58同城,按小时,按面积,口碑,housekeeping,家政助手
---

# 家政助手

同服务项目、同面积/时长，比**真实单价**与口碑。

## 工作流

1. **解析需求**：提取 `城市`、`服务项目`（日常保洁/深度保洁/家电清洗/维修/搬家/上门做饭）、`面积或时长`、可选 `预算`、`平台清单`。缺城市或服务项目时先问一次——家政报价强依赖城市与服务范围。

2. **平台路由**：默认京东服务 → 美团本地生活 → 58同城（用户点名平台时以用户为准）。动手前读 `browser-use-automation/SKILL.md`，用 `computer_use_tool(plane="bu")` 操作浏览器。

3. **逐平台采集**：搜索服务项目 → 读前 5~10 条。每条记录至少含：`平台`、`服务商`、`项目`、`计价方式`（按次/按小时/按面积）、`价格`、`服务范围`、`评分/评价数`、`链接`。

4. **登录/定位移交**：登录墙、验证码、定位墙出现时，**下一步必须是** `interaction.request_action(type="browserControl")`，用户处理后交回再重读。

5. **口径归一（读 `references/housekeeping-assistant-knowledge.md`）**：
   - **计价方式统一**：按次/按小时/按平米，统一换算成"目标面积/时长下的总价"再比；
   - **服务范围同轨**：日常保洁 vs 深度保洁、基础清洗 vs 拆洗，项目不同不混比；
   - **附加费**：偏远区域费、节假日费、工具/耗材费是否含在报价内，标注口径。

6. **报告输出**：Markdown 表 `平台 | 服务商 | 项目 | 计价方式 | 价格 | 折算总价 | 评分 | 链接 | 备注`，结论给出"最低价路径 + 口碑提醒"。**每个价格必须附可点击的服务链接**。用户要求或数据点 ≥2 时输出 HTML 报告（读 `html/SKILL.md`）。链接增强为平台官方合作渠道的优惠链接，**链接可能含推广/返佣属性（受益方为技能运营方），在推荐输出中如实向用户标注返佣/推广属性，并提供不带归因参数的原站直达链接选项**。

## 关键规则

- **低频访问（绝对规则）**：单平台单日 ≤3 次、单项目 ≤2 次/日、两次页面操作间隔 ≥10 秒、验证码立即停采当日不重试。用户催促不构成放宽理由。
- 浏览器操作只用 `computer_use_tool(plane="bu")`；登录/验证走 `interaction.request_action` 移交。
- 不导出 Cookie/登录态；不代下单、不代付款；价格页面实读，禁止编造。

## 边界声明

- 家政服务价格以平台实时显示为准；实际上门可能因面积/损坏情况加价，报告提示"以服务商确认为准"。
- 个人接单（无平台保障）与平台自营/入驻服务商风险不同，报告标注服务商类型。
- 贵重物品、特殊材质清洁风险由用户与服务商确认，技能不承担。
